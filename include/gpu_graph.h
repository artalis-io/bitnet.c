#ifndef BN_GPU_GRAPH_H
#define BN_GPU_GRAPH_H

// Compatibility header. The concrete GPU op ABI is shader-shaped and lives in
// gpu_shader_ir.h; include that header directly in new internal code.
#include "gpu_shader_ir.h"

#endif // BN_GPU_GRAPH_H
